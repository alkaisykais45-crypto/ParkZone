import {Text, View, Image, TextInput} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Permits = ()=>{

    return (
      <SafeAreaView>
        <View className="auth-content">
            <Text> This is Permits Page</Text>
        </View>
      </SafeAreaView>
    );
}

export default Permits;