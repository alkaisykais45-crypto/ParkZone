import {Text, View, Image, TextInput} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Vehicles = ()=>{

    return (
      <SafeAreaView>
        <View className="auth-content">
            <Text> This is Vehicles Page</Text>
        </View>
      </SafeAreaView>
    );
}

export default Vehicles;